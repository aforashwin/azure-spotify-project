# The main pipeline for dbxspotify_dab

resources:
  pipelines:
    dbxspotify_dab_etl:
      name: dbxspotify_dab_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/dbxspotify_dab_etl"

      libraries:
        - glob:
            include: ../src/dbxspotify_dab_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
